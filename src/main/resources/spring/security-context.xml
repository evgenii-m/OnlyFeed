<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
            http://www.springframework.org/schema/security
            http://www.springframework.org/schema/security/spring-security.xsd">

    <global-method-security pre-post-annotations="enabled"/>

    <http use-expressions="true">
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/feed" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/source" access="hasRole('ROLE_USER')"/>
        <form-login login-page="/" default-target-url="/feed" authentication-failure-url="/loginfail"/>
        <logout logout-success-url="/"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                authorities-by-username-query="
                    SELECT users.email, roles.role FROM users
                    JOIN user_roles on users.id = user_roles.user_id
                    JOIN roles on user_roles.role_id = roles.id
                    WHERE users.email = ?"
                users-by-username-query="
                    SELECT email, password, enabled FROM users WHERE email = ?"
            />
        </authentication-provider>
    </authentication-manager>

</beans:beans>